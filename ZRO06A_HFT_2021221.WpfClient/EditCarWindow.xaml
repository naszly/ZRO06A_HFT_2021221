<Window x:Class="ZRO06A_HFT_2021221.WpfClient.EditCarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZRO06A_HFT_2021221.WpfClient"
        mc:Ignorable="d"
        Title="EditCarWindow" Height="400" Width="250">
    <Window.DataContext>
        <local:EditCarWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="13*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <Label Content="Model"/>
            <TextBox Text="{Binding Car.Model}"/>
            <Label Content="Base Price"/>
            <TextBox Text="{Binding Car.BasePrice}"/>
            <Label Content="Brands"/>
            <ComboBox ItemsSource="{Binding Brands}" SelectedItem="{Binding SelectedBrand}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <Label Content="{Binding Name}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        <Button Grid.Row="1" Content="Ok" Click="Button_Click"/>
    </Grid>
</Window>
